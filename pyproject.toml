[tool.poetry]
name = "pypip"
version = "0.1.0"
description = ""
authors = ["DonkeyBoard <119456795+WillDenby@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
typer = "^0.9.0"
virtualenv = "^20.25.0"
toml = "^0.10.2"
packaging = "^23.2"
poetry = "^1.7.1"

[tool.poetry.scripts]
pypip = "pypip.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
